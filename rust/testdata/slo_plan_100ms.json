{
  "note": "Rust plan SLO: cli_wall_ms < 100 on cache-miss with warm DB schema (Docker SQL 2019 + .temp/sql)",
  "max_cli_wall_ms": 100,
  "requires": ["RMIG_RUN_SQLSERVER_INTEGRATION=1", "co-located SQL RTT", "L1 invalidated before run"]
}
